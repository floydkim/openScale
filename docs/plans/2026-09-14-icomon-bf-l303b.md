# ICOMON BF-L303B Support Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a narrowly matched, tested Kotlin GATT handler for the ICOMON BF-L303B (`Body scale` / `FI2019LB-B`) and prepare a focused branch for an upstream openScale pull request.

**Architecture:** Reuse the existing `ScaleDeviceHandler` transport and registry. Let the new handler decode the ICOMON 20-byte `A2`/`A3` frames, send the observed `B1` profile, acknowledge `A0`/`A1`/`A3`, and publish only the values present in those frames.

**Tech Stack:** Kotlin, Android BLE through openScale's Blessed adapter, JUnit/Robolectric, Google Truth, Gradle.

---

## 1. Create the implementation branch

From the clean `master` checkout, create `feat/add-icomon-bf-l303b-handler`. Keep the design and implementation-plan commits on the separate plan branch so the eventual PR contains only the feature.

Commands:

```sh
cd /Users/floydkim/projects/contribution/openScale
git switch master
git switch -c feat/add-icomon-bf-l303b-handler
```

Expected result: the new branch starts at the upstream fork's current `master` and the worktree is clean.

## 2. Add protocol tests first

Files:

- Add `android_app/app/src/test/java/com/health/openscale/core/bluetooth/scales/IcomonBodyScaleHandlerTest.kt`.

Tests to write before production code:

1. A synthetic valid `A2` frame decodes its state and 24-bit big-endian gram weight.
2. A synthetic valid `A3` frame decodes weight, heart rate, and impedance.
3. A bad checksum and an incomplete/oversized frame are rejected without throwing from the handler-facing parser.
4. `B0` acknowledgement frames acknowledge the incoming sequence and use the `0x1F` checksum.
5. `B1` profile frames encode a valid height and male/female flag.
6. `Body scale` with or without an advertised `0xFFB0` service is claimed, while a generic `ICOMON`/`swan`/`yg` device remains unclaimed by this handler.

Run from `android_app/`:

```sh
./gradlew testDebugUnitTest --tests '*IcomonBodyScale*'
```

Expected initial result: the new tests fail to compile because the handler does not yet exist. Do not add production exports solely for tests; use the same `internal`/companion helpers used by existing scale tests.

## 3. Implement the ICOMON handler

File:

- Add `android_app/app/src/main/java/com/health/openscale/core/bluetooth/scales/IcomonBodyScaleHandler.kt`.

Implementation details:

- Match exact advertised name `Body scale` (case-insensitive). The device can omit `0xFFB0` from its advertisement, so use the verified `0xFFB0` service after connecting instead of requiring it during scanning.
- Declare `LIVE_WEIGHT_STREAM`, `BODY_COMPOSITION`, and `USER_SYNC`; implement only `LIVE_WEIGHT_STREAM` and `USER_SYNC` because the observed frames contain no body-composition fields.
- In `onConnected`, reset session state, subscribe to `0xFFB2` and `0xFFB3`, send `B1` only when the user height is valid, and show the existing step-on hint.
- Parse only complete, checksum-valid 20-byte frames using small handler-local helpers.
- Use `A2` state `0x03` as the stable-weight fallback and show progress for other positive weights.
- Use `A3` as the authoritative publication. Publish weight, optional non-zero heart rate, and impedance.
- Acknowledge only incoming `A0`, `A1`, and `A3` frames, matching the observed client behavior.
- If a stable `A2` was received but no `A3` arrived, publish a weight-only measurement on disconnect once; do not duplicate a published `A3`.
- Do not add history, time-sync, unit-change, battery, or generic `FFB0` behavior.

Run:

```sh
./gradlew testDebugUnitTest --tests '*IcomonBodyScale*'
```

Expected result: protocol and matching tests pass.

Commit proposal: `feat: add ICOMON BF-L303B scale support`.

## 4. Register the handler and catalog fixture

Files:

- Update `android_app/app/src/main/java/com/health/openscale/core/bluetooth/ScaleFactory.kt`.
- Update `android_app/app/src/test/java/com/health/openscale/core/bluetooth/ScaleCatalog.kt`.
- Extend `ScaleFactoryTest` only if the catalog fixture does not already cover the required collision/order assertion.

Implementation:

- Import the handler and register it immediately before `MGBHandler`.
- Add one synthetic `Body scale` + `0xFFB0` fixture owned by `IcomonBodyScaleHandler`.
- Add the exact-name single-claim assertion and the ordering assertion against `MGBHandler`.
- Do not add a raw capture, personal address, or personal measurement to the repository.

Run:

```sh
./gradlew testDebugUnitTest --tests '*ScaleCatalogTest' --tests '*ScaleFactoryTest'
```

Expected result: the fixture resolves to the new handler, generic ICOMON remains with `MGBHandler`, and the generated catalog reports the new GATT device.

Commit proposal: `feat: register ICOMON BF-L303B in the scale catalog`.

## 5. Full verification and PR preparation

From `android_app/` run:

```sh
./gradlew testDebugUnitTest
./gradlew assembleDebug
```

Then from the repository root:

```sh
git diff --check
git status --short --branch
git log --oneline --decorate -5
```

Review the diff for accidental captures, binaries, MAC addresses, proprietary assets, or unrelated changes. Confirm unsupported operations are not implemented and that the branch is based on `master`.

Push only the feature branch to the fork:

```sh
git push -u origin feat/add-icomon-bf-l303b-handler
```

Do not open the pull request automatically in this task; provide the fork branch and compare URL so the user can inspect it before creating a PR against `oliexdev/openScale:master`.
